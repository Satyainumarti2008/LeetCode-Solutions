class Solution {
public:
    int mySqrt(int x) {
        // int l=0,r=x;
        // while(l<=r){
        //     int mid=l+(r-l)/2;
        //     if(sqrt(mis))
        // }
        return sqrt(x);
    }
};